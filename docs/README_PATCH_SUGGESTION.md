# README patch suggestion

Replace the current Paper-1-only opening with something like:

```markdown
# Selective Computation and Residual Dynamics

Mechanism-first research line for studying selective influence over neural state dynamics.

Paper 0 defines the broader dual-selection framework:

- memory selection: which available information becomes computationally active;
- computational selection: which candidate transformations become effective state updates.

Paper 1 instantiates the framework in Transformer residual streams, testing refinement, complementarity, interference, goal-conditioned gating, and sufficient activation.

See:

- `docs/papers/paper0/`
- `docs/papers/paper1/`
- `docs/METRICS.md`
- `docs/ROADMAP.md`
```
